
export enum Tag {
    Stat,
    Debuff,
    Buff,
    Bonus,
    Malus
}

export enum Target {
    Allies,
    Ally,
    Ennemies,
    Ennemy,
    Me,
    Everyone
}

export enum Buffable {
    Speed = 'speed',
    Attack = 'attack'
}

export enum VectorsName {
    Card = 'card',
    Avatar = 'avatar',
    GuildIcon = 'guildIcon',
    GuildName = 'guildName',
    InfoTop = 'infoTop',
    InfoBottom = 'infoBottom',
    Body = 'body'
}